apply plugin: 'li-java'

dependencies {
  compile spec.product.hive.'hive-common'
  compile spec.product.hive.'hive-metastore'
  compile(spec.product.hive.'hive-exec') {
    exclude group: 'jline', module: 'jline'
  }
}

artifacts {
  archives jar, javadocJar, sourcesJar
}
